# 2.4 — AWS Development 의 시장 데이터 실제 상태

## 언제 · 어디서

- 2026-08-08
- 환경: AWS Development (계정 `418553863687`, `ap-northeast-2`)
- 신원: `arn:aws:iam::418553863687:user/admin-Pearwon99` (`aws login` 세션)
- 루트 커밋: `c792ead` (develop)

## 실행한 명령

```powershell
aws sts get-caller-identity --profile idea2strategy-terraform
aws rds describe-db-instances --profile idea2strategy-terraform
aws s3 ls s3://idea2strategy-dev-418553863687-market-data/ --recursive --summarize --profile idea2strategy-terraform
aws s3 cp s3://.../market-catalog-bootstrap/.../apply-receipt.json - --profile idea2strategy-terraform
```

## 관찰한 결과

### 네 해상도 모두 존재한다 — 30분봉 포함

S3 객체 키의 `resolution=` 파티션 집계:

| 해상도 | 객체 수 |
| --- | --- |
| 30m | 193 |
| 1h | 192 |
| 4h | 192 |
| 1d | 192 |

`historical/` 접두사에 768개, 버킷 전체 910개 객체 · 1.87 GB. 경로 형태는
`historical/provider=alpaca/feed=sip/adjustment=all/session=regular/resolution=<r>/revision=00000001/year=<y>/shard=<n>-of-08/manifest_id=<uuid>/part-00001.parquet` 이며
연도·샤드로 분할돼 있다(종목은 파케이 내부).

### 카탈로그 부트스트랩은 이미 적용됐다

`market-catalog-bootstrap/.../apply-receipt.json` (2026-08-05 18:28):

```
status: passed, phase: Apply
applied.status: ALREADY_APPLIED
verified_object_count: 768, manifest_count: 96, inserted_row_count: 0
```

대상 DB 테이블 수:

| 테이블 | 행 수 |
| --- | --- |
| `market_data.instruments` | **725** |
| `market_data.instrument_symbols` | 725 |
| `market_data.trading_sessions` | 2,658 |
| `market_data.dataset_manifests` | **96** |
| `market_data.dataset_objects` | 768 |
| `storage.objects` | 768 |
| `market_data.feature_definitions` | 1 |
| **`market_data.feature_materializations`** | **0** |

### RDS

`idea2strategy-dev-postgres`, PostgreSQL **16.13**, status `available`, DB `idea2strategy`.

### SQS

백테스트 3레인(+요청·DLQ)과 방 원장 큐가 이미 존재한다:
`idea2strategy-dev-room-ledger-opened`, `idea2strategy-dev-room-ledger-open-rejected`,
`idea2strategy-dev-corporate-action-approval`.
**로컬 이름과 다르다** — 로컬은 `room-ledger-rejected`, AWS 는 `room-ledger-open-rejected`.
환경별 URL 로 주입되므로 동작에는 문제가 없으나, 두 곳을 비교할 때 혼동하기 쉽다.

## 이 증거가 정정하는 것

**계획서 §2.4 의 전제가 AWS 에는 맞지 않는다.** §2.4 는 "시장 데이터가 적재되지 않았고,
실제로 쓴 수집기가 30분봉을 버린다" 고 적었다. 앞부분은 **로컬에만** 참이다. AWS 에는 네
해상도가 모두 있고 종목 725개와 manifest 96개가 등록돼 있으며 부트스트랩은
`ALREADY_APPLIED` 다.

`market_hist_script` 가 30분봉을 버린다는 지적 자체는 여전히 유효하다 — 다만 그 스크립트는
AWS 적재 경로가 아니었다. 적재는 `development-market-catalog-bootstrap` 이 했고 30분봉을
포함한다. 따라서 그 스크립트를 적재 경로에서 빼야 한다는 결론은 유지되지만, **긴급도는
훨씬 낮다** — 지금 쓰이고 있지 않기 때문이다.

## 통과하지 못한 것

1. **`feature_materializations` 는 AWS 에서도 0 이다.** §2.5(지표 백필)는 로컬·AWS 양쪽에서
   여전히 미완이며, 이것이 RSI 전략의 실질 차단 요인이다. 종목 725개 × 네 해상도의 백필이
   남아 있다.
2. **`feature_definitions` 가 AWS 에서 1개다.** 로컬은 마이그레이션 적용 후 5개(레거시 1m +
   프로덕션 30m·1h·4h·1d)인데, AWS 는 1개다 — 즉 **AWS RDS 가 최신 마이그레이션을 받지
   않았다.** `V20260808120100__pipeline_seed_production_rsi_timeframes.sql` 이 적용되면 4개가
   추가된다. 백필보다 이것이 먼저다.
3. **RDS 직접 조회는 하지 못했다.** VPC 안에 있어 이 기계에서 psql 이 닿지 않는다. 위 테이블
   수치는 2026-08-05 영수증의 값이며 그 시점의 사실이다. 현재 값 확인은 SSM Run Command 나
   배포 워크플로를 거쳐야 한다.
