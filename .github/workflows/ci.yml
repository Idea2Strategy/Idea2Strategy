name: CI

on:
  pull_request:
    branches: [develop]
  push:
    branches: [develop]

permissions:
  contents: read

concurrency:
  group: root-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  schema-and-coordination:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
          submodules: false
      - uses: pnpm/action-setup@v4
        with:
          version: 11.9.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - name: Install schema tooling
        run: pnpm install --frozen-lockfile
      - name: Validate canonical DBML
        run: pnpm dbml:validate
      - name: Test dbdiagram export preparation
        run: pnpm dbml:export:test
      - name: Validate tracked formatting
        run: git diff --check HEAD^
      - name: Verify submodule pointers are tracked
        shell: bash
        run: |
          set -euo pipefail
          for path in backend backtest-engine data-pipeline trading-engine ui; do
            test "$(git ls-files -s "$path" | awk '{print $1}')" = "160000"
          done

  flyway-integration:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          submodules: false
      - name: Resolve exact service gitlinks
        id: service-gitlinks
        shell: bash
        run: |
          echo "backend=$(git rev-parse HEAD:backend)" >> "$GITHUB_OUTPUT"
          echo "trading=$(git rev-parse HEAD:trading-engine)" >> "$GITHUB_OUTPUT"
      - name: Checkout exact backend gitlink
        uses: actions/checkout@v4
        with:
          repository: Idea2Strategy/Idea2Strategy-backend
          ref: ${{ steps.service-gitlinks.outputs.backend }}
          path: backend
          token: ${{ secrets.SUBMODULE_READ_TOKEN }}
          persist-credentials: false
      - name: Checkout exact trading gitlink
        uses: actions/checkout@v4
        with:
          repository: Idea2Strategy/Idea2Strategy-trading-engine
          ref: ${{ steps.service-gitlinks.outputs.trading }}
          path: trading-engine
          token: ${{ secrets.SUBMODULE_READ_TOKEN }}
          persist-credentials: false
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle
          cache-dependency-path: |
            backend/*.gradle.kts
            backend/gradle.properties
            backend/gradle/wrapper/gradle-wrapper.properties
      - name: Verify generated Flyway Compose boundary
        shell: pwsh
        run: ./scripts/test-docker-development.ps1
      - name: Verify central Flyway migration lifecycle
        shell: pwsh
        run: ./scripts/test-flyway-migration.ps1
